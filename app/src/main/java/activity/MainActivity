package com.dominant.fplus.activity;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import com.dominant.fplus.R;
import com.dominant.fplus.helper.SnackBar;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        SnackBar.makeShort(MainActivity.this, "Hey, I am snack bar");
        SnackBar.makeLong(MainActivity.this, "I am long snack bar");
    }
}
